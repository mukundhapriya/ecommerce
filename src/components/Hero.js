function Hero() {
    return ( <
        div className = "hero" >
        <
        h1 > Fresh Groceries Delivered < /h1> <
        p > Vegetables, Fruits & Daily Essentials < /p> <
        button > Shop Now < /button> <
        /div>
    );
}

export default Hero;